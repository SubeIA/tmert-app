import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cl.achs.tmertapp',
  appName: 'tmert-app',
  webDir: 'dist/tmert-app/browser',
};

export default config;
